default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: st
  schema_search_path: public

test:
  <<: *default
  database: pozas_test
  schema_search_path: public
